# Production Database Security (for when you move to PostgreSQL/MySQL)
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_APP_USER:app_user}
spring.datasource.password=${DB_APP_PASSWORD}

# Production Database Security Settings
spring.datasource.hikari.data-source-properties.useSSL=true
spring.datasource.hikari.data-source-properties.requireSSL=true
spring.datasource.hikari.data-source-properties.verifyServerCertificate=true

# Disable dangerous features in production
spring.h2.console.enabled=false
spring.jpa.show-sql=false